name: Build TrollStore IPA
on:
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-latest
    steps:
      - name: 📥 Récupérer le code
        uses: actions/checkout@v3

      - name: ⚙️ Installer Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 18

      - name: 📦 Installer les dépendances
        run: npm install

      - name: 🔧 Expo Prebuild
        run: npx expo prebuild --platform ios

      - name: 🔨 Construire l'App (Unsigned ARM64)
        run: |
          cd ios
          xcodebuild -workspace *.xcworkspace -scheme $(ls -d *.xcworkspace | cut -d. -f1) -configuration Release -sdk iphoneos CODE_SIGNING_ALLOWED=NO clean build archive -archivePath ../build/App.xcarchive
      
      - name: 🎁 Créer l'IPA
        run: |
          mkdir Payload
          cp -r build/App.xcarchive/Products/Applications/*.app Payload/
          zip -r MySecretApp.ipa Payload

      - name: 📤 Téléverser l'IPA
        uses: actions/upload-artifact@v4
        with:
          name: TrollStore-IPA
          path: MySecretApp.ipa